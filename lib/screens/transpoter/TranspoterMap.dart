import 'package:flutter/material.dart';

class TranspoterMap extends StatelessWidget {
  const TranspoterMap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
